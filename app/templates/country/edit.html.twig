{% include 'default/_form.html.twig' with {
    page_title: 'title.country_edit',
    page_title_param: {'%id%': country.id},
    attr: {
        method: 'PUT',
        action: url('country_edit', {'id': country.id}),
    },
    back_to_list_path: 'country_index',
} %}