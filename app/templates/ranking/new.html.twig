{% include 'default/_form.html.twig' with {
    page_title: 'title.ranking_new',
    attr: {
        method: 'POST',
        action: url('ranking_new', {destination_id: destination})
    },
    back_to_list_path: 'destination_index',
} %}