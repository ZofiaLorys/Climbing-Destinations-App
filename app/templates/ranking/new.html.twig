{% include 'default/_form.html.twig' with {
    page_title: 'title.ranking_new',
    attr: {
        method: 'POST',
        action: url('ranking_new')
    },
    back_to_list_path: 'ranking_index',
} %}