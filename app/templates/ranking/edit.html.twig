{% include 'default/_form.html.twig' with {
    page_title: 'title.ranking_edit',
    page_title_param: {'%id%': ranking.id},
    attr: {
        method: 'PUT',
        action: url('ranking_edit', {'id': ranking.id}),
    },
    back_to_list_path: 'ranking_index',
} %}