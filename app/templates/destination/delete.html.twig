{% include 'default/_form.html.twig' with {
    page_title: 'title.destination_delete',
    page_title_param: {'%id%': destination.id},
    attr: {
        method: 'DELETE',
        action: url('destination_delete', {'id': destination.id}),
    },
    back_to_list_path: 'destination_index',
} %}