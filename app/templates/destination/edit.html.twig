{% include 'default/_form.html.twig' with {
    page_title: 'title.destination_edit',
    page_title_param: {'%id%': destination.id},
    attr: {
        method: 'PUT',
        action: url('destination_edit', {'id': destination.id}),
    },
    back_to_list_path: 'destination_index',
} %}