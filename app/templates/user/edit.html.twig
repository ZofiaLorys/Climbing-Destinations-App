{% include 'default/_form.html.twig' with {
    page_title: 'title.user_edit',
    page_title_param: {'%id%': ranking.id},
    attr: {
        method: 'PUT',
        action: url('user_edit', {'id': ranking.id}),
    },
    back_to_list_path: 'user_index',
} %}
